#cloud-config
# =============================================================================
# infra/cloud-init.yaml.tftpl
#
# WireGuard VPN server provisioning via wg-easy (Docker container).
# Sets up persistent storage, generates admin peer config, and enables admin UI.
#
# Script content for write_files entries is rendered separately (see main.tf
# locals) and injected here via templatefile variables:
#   - sshd_hardening_conf       <- scripts/sshd-hardening.conf       (static)
#   - provision_wireguard_script <- scripts/provision-wireguard.sh.tftpl (terraform vars)
#   - provision_shell_script     <- scripts/provision-shell.sh       (static)
# =============================================================================

package_update: true
package_upgrade: true

apt:
  conf: |
    Dpkg::Options { "--force-confdef"; "--force-confold"; };
    APT::Get::Assume-Yes "true";

packages:
  - curl
  - jq
  - tmux
  - ufw
  - fail2ban
  - whois
  - wireguard-tools
  - docker.io
  - docker-compose

write_files:
  # SSH hardening
  - path: /etc/ssh/sshd_config.d/99-hardening.conf
    content: |
      ${indent(6, sshd_hardening_conf)}

  # Main provisioning script
  - path: /root/provision-wireguard.sh
    permissions: "0755"
    content: |
      ${indent(6, provision_wireguard_script)}

  # Shell setup
  - path: /root/provision-shell.sh
    permissions: "0755"
    content: |
      ${indent(6, provision_shell_script)}

runcmd:
  - export DEBIAN_FRONTEND=noninteractive
  - export NEEDRESTART_MODE=a

  # System hardening
  - ufw default deny incoming
  - ufw default allow outgoing
  - ufw allow 22/tcp
  - ufw allow ${wg_server_port}/udp
  - ufw allow 51821/tcp
  - ufw --force enable
  - systemctl restart ssh
  - systemctl enable fail2ban
  - systemctl start fail2ban

  # WireGuard provisioning
  - /root/provision-wireguard.sh

  # Shell setup
  - /root/provision-shell.sh

final_message: |
  Cloud-init complete. WireGuard server is running.

  Next steps:
  1. Access admin UI:
     ssh -L 127.0.0.1:51821:127.0.0.1:51821 root@<server-ip>
     Then open http://127.0.0.1:51821

  2. Download admin client config:
     scp root@<server-ip>:/root/wireguard-admin.conf ./

  3. Import the .conf into your WireGuard client and connect!
