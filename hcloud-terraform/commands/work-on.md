Read ~/repos.yaml and find the repo matching "$ARGUMENTS".

1. If the repo is not cloned at its configured path, clone it.
2. cd into the repo directory.
3. Run any setup commands if they haven't been run yet.
4. Confirm you are ready and show a brief summary: repo name, stack, and any notes.
